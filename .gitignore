# Système
.DS_Store
Thumbs.db
desktop.ini

# Éditeurs
.vscode/
*.code-workspace

# Node (si utilisé plus tard)
node_modules/

# Claude Code
.claude/

# Uploads (gérés par l'admin, pas par git)
uploads/*
!uploads/.htaccess

# Contenu administrable (géré côté serveur, pas par git)
content.json
content.json.bak

# Données stats (gérées côté serveur)
data/
